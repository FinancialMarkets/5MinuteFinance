### We have presentations that we want to deploy as both isoslides and as html documents (which play nice on mobile).  However we want to maintain one .Rmd file.  So this function will prepend the appropriate YAML header to the Rmd and deploy.


chooseDeploy <- function(yamlFile, rmdFile, theappName){


    }
